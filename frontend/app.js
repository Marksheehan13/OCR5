const pages=[...document.querySelectorAll('.page')];
const nav=[...document.querySelectorAll('[data-page]')];
const crumb=document.getElementById('crumb');
const API_BASE=window.OCR5_API_BASE||'';

const liveStyle=document.createElement('style');liveStyle.textContent=`
.live-overlay{position:fixed;inset:0;z-index:9999;display:grid;place-items:center;padding:24px}.live-overlay.hidden{display:none}.live-backdrop{position:absolute;inset:0;background:rgba(9,10,16,.72);backdrop-filter:blur(16px)}.live-modal{position:relative;width:min(960px,96vw);max-height:92vh;overflow:auto;background:#fbfbfd;border:1px solid rgba(255,255,255,.5);border-radius:28px;box-shadow:0 40px 100px rgba(0,0,0,.32);color:#171820}.live-modal-head,.review-head{display:flex;justify-content:space-between;gap:24px;padding:32px 36px 24px}.live-modal-head h2,.review-head h2{margin:4px 0 7px;font:800 30px/1.1 Manrope,sans-serif;letter-spacing:-.04em}.live-modal-head p,.review-head p{margin:0;color:#737783}.live-kicker{font:700 10px/1 DM Sans,sans-serif;letter-spacing:.16em;color:#735cff}.live-close{width:38px;height:38px;border:1px solid #e7e8ed;border-radius:12px;background:#fff;font-size:24px;color:#737783;cursor:pointer}.upload-dropzone{margin:8px 36px 22px;min-height:300px;border:1.5px dashed #cfd1dc;border-radius:22px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:9px;background:linear-gradient(145deg,#fff,#f5f4fb);transition:.2s}.upload-dropzone.dragging{border-color:#735cff;background:#f2efff;transform:scale(.995)}.upload-orb,.processing-orb{width:58px;height:58px;display:grid;place-items:center;border-radius:18px;background:linear-gradient(135deg,#8c79ff,#5d45e8);color:white;font-size:25px;box-shadow:0 14px 30px rgba(103,79,232,.25);margin-bottom:7px}.upload-dropzone>b{font:800 19px Manrope}.upload-dropzone>span,.upload-dropzone>small{color:#81848f}.upload-dropzone>small{font-size:10px;letter-spacing:.1em;margin-top:5px}.upload-dropzone .primary{margin-top:12px}.provider-row{display:grid;grid-template-columns:1fr 1.5fr;gap:14px;padding:0 36px 32px}.provider-row label{display:grid;gap:7px;font:600 12px DM Sans;color:#666975}.provider-row input,.provider-row select{height:46px;border:1px solid #dedfe6;border-radius:12px;padding:0 13px;background:white;color:#20222a;font:500 13px DM Sans;outline:none}.processing-screen{padding:72px 54px;text-align:center}.processing-screen .processing-orb{margin:0 auto 22px;animation:float 1.8s ease-in-out infinite}.processing-screen h2{font:800 34px Manrope;margin:8px 0}.processing-screen p{color:#777b86}.process-list{margin:28px auto 0;max-width:640px;text-align:left;display:grid;gap:8px}.process-row{display:grid;grid-template-columns:18px 1fr auto;align-items:center;gap:12px;padding:13px 15px;border:1px solid #e8e8ed;border-radius:13px;background:white}.process-row small{color:#767a86}.process-dot{width:9px;height:9px;border-radius:50%;background:#735cff;color:#fff;font-size:9px;display:grid;place-items:center}.process-row.failed .process-dot{background:#df665f}.review-shell{min-height:650px}.review-body{display:grid;grid-template-columns:42% 58%;border-top:1px solid #ececf0}.document-panel{padding:28px;background:#f1f1f5;min-height:560px}.document-placeholder{height:100%;min-height:500px;border:1px solid #dddde4;border-radius:18px;background:linear-gradient(150deg,#fff,#f8f8fa);display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:30px;color:#777a84}.document-placeholder .doc-icon{width:76px;height:96px;border:1px solid #d9dae0;border-radius:9px;background:white;display:grid;place-items:center;font-size:30px;color:#735cff;box-shadow:0 10px 30px rgba(0,0,0,.06);margin-bottom:18px}.document-placeholder b{color:#252730;font:700 15px Manrope}.document-placeholder span{margin-top:6px;font-size:12px}.document-placeholder small{max-width:250px;margin-top:18px;line-height:1.6}.review-panel{padding:28px 30px;overflow:auto}.review-summary{display:flex;justify-content:space-between;align-items:center;padding:18px;border:1px solid #e7e7ec;border-radius:17px;background:white;margin-bottom:18px}.review-summary span{display:block;color:#7b7e89;font-size:11px}.review-summary strong{display:block;font:800 29px Manrope;margin-top:4px}.review-ring{--score:90;width:54px;height:54px;border-radius:50%;background:conic-gradient(#735cff calc(var(--score)*1%),#ececf1 0);display:grid;place-items:center;position:relative}.review-ring:after{content:'';position:absolute;inset:5px;background:white;border-radius:50%}.review-ring span{position:relative;z-index:1;font:800 11px Manrope}.review-fields{display:grid;grid-template-columns:1fr 1fr;gap:12px}.review-field{display:grid;gap:7px}.review-field>span{font:600 11px DM Sans;color:#626570;display:flex;justify-content:space-between}.review-field input{height:43px;border:1px solid #dedfe6;border-radius:11px;padding:0 12px;background:#fff;font:600 13px DM Sans;outline:none}.review-field input:focus{border-color:#8a78ee;box-shadow:0 0 0 3px rgba(115,92,255,.1)}.review-field small{font-size:9px;color:#9699a2}.confidence{font-style:normal}.confidence.high{color:#3f9b6d}.confidence.medium{color:#b47b28}.confidence.review{color:#d35d57}.review-warning{display:grid;gap:5px;margin:16px 0;padding:13px 14px;border-radius:13px;background:#fff5e6;color:#8d641f}.review-warning b{font-size:12px}.review-warning span{font-size:11px}.line-items-review{margin-top:18px}.section-title{display:flex;justify-content:space-between;margin-bottom:9px;font-size:12px}.section-title span{color:#888b95}.line-review{display:grid;grid-template-columns:22px 1fr 50px 70px 38px;gap:8px;align-items:center;padding:10px 0;border-bottom:1px solid #eeeef2;font-size:11px}.line-review>span:first-child{color:#9a9ca5}.line-review>b{font-weight:600}.line-review em{font-style:normal;color:#4b9b72}.review-actions{display:flex;justify-content:flex-end;gap:10px;margin-top:22px}.review-actions button{height:43px;padding:0 17px;border-radius:11px}.review-actions .secondary{border:1px solid #dedfe5;background:white}.ocr5-toast{position:fixed;right:24px;bottom:24px;z-index:10001;background:#171820;color:white;border-radius:13px;padding:13px 17px;font:600 12px DM Sans;box-shadow:0 18px 40px rgba(0,0,0,.2);transform:translateY(15px);opacity:0;pointer-events:none;transition:.25s}.ocr5-toast.show{transform:translateY(0);opacity:1}.ocr5-toast.error{background:#9d403c}@keyframes float{50%{transform:translateY(-7px)}}@media(max-width:760px){.live-overlay{padding:8px}.live-modal{width:100%;max-height:96vh;border-radius:20px}.live-modal-head,.review-head{padding:24px 20px 18px}.upload-dropzone{margin:8px 20px 18px}.provider-row{grid-template-columns:1fr;padding:0 20px 24px}.review-body{grid-template-columns:1fr}.document-panel{min-height:220px;padding:16px}.document-placeholder{min-height:190px}.review-panel{padding:20px}.review-fields{grid-template-columns:1fr}.line-review{grid-template-columns:20px 1fr 48px 55px 34px}}
`;document.head.appendChild(liveStyle);

function show(name){pages.forEach(page=>page.classList.toggle('hidden',page.id!==name));document.querySelectorAll('.nav[data-page]').forEach(item=>item.classList.toggle('active',item.dataset.page===name));if(crumb)crumb.textContent=name[0].toUpperCase()+name.slice(1);window.scrollTo({top:0,behavior:'smooth'});if(name==='invoices')loadLiveInvoices();if(name==='analytics')loadLiveAnalytics();}
document.querySelectorAll('[data-page]').forEach(item=>item.addEventListener('click',event=>{event.preventDefault();show(item.dataset.page);}));
const state={invoice:null,apiKey:localStorage.getItem('ocr5_api_key')||'',provider:localStorage.getItem('ocr5_provider')||'Google Gemini (free tier)'};
function esc(value){return String(value??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));}
function money(value,currency='EUR'){const n=Number(value);return Number.isFinite(n)?new Intl.NumberFormat('en-IE',{style:'currency',currency}).format(n):'—';}
function api(path,options={}){return fetch(`${API_BASE}${path}`,options).then(async r=>{const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.detail||`Request failed (${r.status})`);return data;});}
function ensureOverlay(){if(document.getElementById('live-overlay'))return document.getElementById('live-overlay');const el=document.createElement('div');el.id='live-overlay';el.className='live-overlay hidden';el.innerHTML='<div class="live-backdrop"></div><div class="live-modal" role="dialog" aria-modal="true"></div>';document.body.appendChild(el);el.querySelector('.live-backdrop').onclick=()=>el.classList.add('hidden');return el;}
function openOverlay(html){const el=ensureOverlay();el.querySelector('.live-modal').innerHTML=html;el.classList.remove('hidden');return el;}function closeOverlay(){document.getElementById('live-overlay')?.classList.add('hidden');}
async function openUploader(){const overlay=openOverlay(`<div class="live-modal-head"><div><span class="live-kicker">OCR5 AI</span><h2>Bring your invoices in.</h2><p>Drop documents here. OCR5 will extract, validate and prepare them for review.</p></div><button class="live-close" data-close>×</button></div><div class="upload-dropzone" id="live-drop"><div class="upload-orb">✦</div><b>Drop invoices here</b><span>or choose files from your computer</span><small>JPG · PNG · WEBP · TIFF · HEIC</small><button class="primary" id="choose-files">Choose invoices</button><input id="live-files" type="file" multiple accept=".jpg,.jpeg,.png,.heic,.heif,.bmp,.tiff,.webp" hidden></div><div class="provider-row"><label>AI provider<select id="live-provider"></select></label><label>API key<input id="live-key" type="password" placeholder="Stored only in this browser session"></label></div>`);overlay.querySelector('[data-close]').onclick=closeOverlay;const provider=overlay.querySelector('#live-provider'),key=overlay.querySelector('#live-key');try{const p=await api('/api/providers');provider.innerHTML=p.providers.map(x=>`<option>${esc(x.name)}</option>`).join('');provider.value=state.provider;}catch{provider.innerHTML=`<option>${esc(state.provider)}</option>`;}key.value=state.apiKey;const input=overlay.querySelector('#live-files');overlay.querySelector('#choose-files').onclick=()=>input.click();const drop=overlay.querySelector('#live-drop');['dragenter','dragover'].forEach(e=>drop.addEventListener(e,x=>{x.preventDefault();drop.classList.add('dragging');}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,x=>{x.preventDefault();drop.classList.remove('dragging');}));drop.addEventListener('drop',e=>processFiles([...e.dataTransfer.files],provider.value,key.value));input.addEventListener('change',()=>processFiles([...input.files],provider.value,key.value));}
async function processFiles(files,provider,key){if(!files.length)return;if(!key){notify('Add your AI API key to process invoices.',true);return;}state.apiKey=key;state.provider=provider;localStorage.setItem('ocr5_api_key',key);localStorage.setItem('ocr5_provider',provider);const overlay=ensureOverlay();overlay.classList.remove('hidden');overlay.querySelector('.live-modal').innerHTML=`<div class="processing-screen"><div class="processing-orb">✦</div><span class="live-kicker">OCR5 INTELLIGENCE</span><h2>Understanding your invoices.</h2><p>OCR5 is extracting, validating and verifying each document.</p><div class="process-list" id="process-list"></div></div>`;const list=overlay.querySelector('#process-list');let first=null;for(const file of files){const row=document.createElement('div');row.className='process-row';row.innerHTML=`<span class="process-dot"></span><span>${esc(file.name)}</span><small>Processing…</small>`;list.appendChild(row);const form=new FormData();form.append('file',file);try{const data=await api('/api/extract',{method:'POST',headers:{'X-OCR5-API-Key':key,'X-OCR5-Provider':provider},body:form});row.querySelector('small').textContent=`${data.invoice.overall_confidence}% confidence`;row.querySelector('.process-dot').textContent='✓';if(!first)first=data.invoice;}catch(err){row.querySelector('small').textContent=err.message;row.classList.add('failed');}}if(first){state.invoice=first;setTimeout(()=>openReview(first),450);}else{notify('No invoices could be processed. Check your provider and API key.',true);}}
function confidenceClass(n){return n>=90?'high':n>=70?'medium':'review';}function fieldEditor(label,key,field){const value=field?.value??'';const confidence=field?.confidence??0;return `<label class="review-field"><span>${label}<em class="confidence ${confidenceClass(confidence)}">${confidence}%</em></span><input data-field="${key}" value="${esc(value)}"><small>${confidence>=90?'High confidence':confidence>=70?'Worth a quick check':'Needs review'}</small></label>`;}
function openReview(invoice){const overlay=openOverlay(`<div class="review-shell"><div class="review-head"><div><span class="live-kicker">INVOICE INTELLIGENCE</span><h2>${esc(invoice.source_file)}</h2><p>OCR5 extracted this document and checked it for consistency.</p></div><button class="live-close" data-close>×</button></div><div class="review-body"><div class="document-panel"><div class="document-placeholder"><div class="doc-icon">▤</div><b>Source document</b><span>${esc(invoice.source_file)}</span><small>The OCR engine has processed the source. A persistent preview will be enabled when the storage viewer is connected.</small></div></div><div class="review-panel"><div class="review-summary"><div><span>Overall confidence</span><strong>${invoice.overall_confidence}%</strong></div><div class="review-ring" style="--score:${invoice.overall_confidence}"><span>${invoice.overall_confidence}</span></div></div><div class="review-fields">${fieldEditor('Supplier','supplier',invoice.supplier)}${fieldEditor('Invoice number','invoice_number',invoice.invoice_number)}${fieldEditor('Date','date',invoice.date)}${fieldEditor('Subtotal','subtotal',invoice.subtotal)}${fieldEditor('VAT amount','vat_amount',invoice.vat_amount)}${fieldEditor('VAT rate','vat_rate',invoice.vat_rate)}${fieldEditor('Total','amount',invoice.amount)}</div>${invoice.warnings?.length?`<div class="review-warning"><b>⚠ Review required</b><span>${esc(invoice.warnings.join(' · '))}</span></div>`:''}<div class="line-items-review"><div class="section-title"><b>Line items</b><span>${invoice.line_items?.length||0} detected</span></div>${(invoice.line_items||[]).map((x,i)=>`<div class="line-review"><span>${i+1}</span><b>${esc(x.description)}</b><span>${esc(x.quantity||'')}</span><span>${esc(x.line_total||'')}</span><em>${x.confidence}%</em></div>`).join('')}</div><div class="review-actions"><button class="secondary" data-close>Back</button><button class="primary" id="save-review">Approve & save</button></div></div></div></div>`);overlay.querySelectorAll('[data-close]').forEach(b=>b.onclick=closeOverlay);overlay.querySelector('#save-review').onclick=()=>saveReviewedInvoice(overlay,invoice);}
async function saveReviewedInvoice(overlay,invoice){const edited=structuredClone(invoice);overlay.querySelectorAll('[data-field]').forEach(input=>{const key=input.dataset.field;if(edited[key])edited[key].value=input.value;});const button=overlay.querySelector('#save-review');button.disabled=true;button.textContent='Checking & saving…';try{let duplicates=[];try{duplicates=(await api('/api/invoices/check-duplicates',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(edited)})).matches||[];}catch(err){if(!err.message.toLowerCase().includes('supabase'))throw err;}if(duplicates.length){button.disabled=false;button.textContent='Approve & save';const first=duplicates[0];if(!confirm(`OCR5 found a possible duplicate: ${first.supplier} · ${money(first.amount,first.currency)}.\n\nSave anyway?`))return;button.disabled=true;button.textContent='Saving…';}const saved=await api('/api/invoices/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(edited)});closeOverlay();show('invoices');notify(`Invoice saved · ${saved.supplier||edited.supplier.value||'Invoice'}`);}catch(err){button.disabled=false;button.textContent='Approve & save';notify(err.message,true);}}
function notify(message,error=false){let n=document.getElementById('ocr5-toast');if(!n){n=document.createElement('div');n.id='ocr5-toast';document.body.appendChild(n);}n.className=`ocr5-toast ${error?'error':''}`;n.textContent=message;n.classList.add('show');setTimeout(()=>n.classList.remove('show'),3500);}
async function loadLiveInvoices(){try{const data=await api('/api/invoices');const rows=data.invoices||[];const container=document.querySelector('#invoices .invoice-table');if(!container||!rows.length)return;const heading=container.querySelector('.heading')?.outerHTML||'';container.innerHTML=heading+rows.map(r=>`<div class="invoice-row clickable live-invoice-row"><span class="supplier"><i class="supplier-logo">${esc((r.supplier||'?')[0].toUpperCase())}</i><b>${esc(r.supplier||'Unknown')}</b></span><span>${esc(r.invoice_number||'—')}</span><span>${esc(r.date||'—')}</span><b>${money(r.amount,r.currency||'EUR')}</b><strong class="confidence">${r.confidence??0}%</strong><span class="status good"><i></i>Saved</span></div>`).join('');}catch{}}
async function loadLiveAnalytics(){try{const data=await api('/api/analytics');document.querySelectorAll('[data-live-total-spend]').forEach(el=>el.textContent=money(data.total_spend));document.querySelectorAll('[data-live-invoice-count]').forEach(el=>el.textContent=data.invoice_count);}catch{}}
document.getElementById('upload')?.addEventListener('click',openUploader);document.getElementById('upload2')?.addEventListener('click',openUploader);document.addEventListener('keydown',event=>{if(event.key==='Escape')closeOverlay();});const search=document.querySelector('.search input');search?.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();document.querySelectorAll('#invoices .invoice-row.clickable').forEach(row=>row.style.display=!q||row.textContent.toLowerCase().includes(q)?'':'none');});show('overview');
